%h1 Products
- for product in @products
  = link_to image_tag(product.image.url(:thumb).to_s), product_path(product.id), :id => "product_#{product.id}_image"
  %p= product.title
  %p= product.description
  %p= product.price

